sample-$(DOWNLOAD_SAMPLE) += download
sample-download-$(DOWNLOAD_SAMPLE) := download.c

sample-$(CLIENT_SAMPLE) += client
sample-client-$(CLIENT_SAMPLE) := client.c

sample-$(STATIC_FILES_SAMPLE) += static-files
sample-static-files-$(STATIC_FILES_SAMPLE) := static-files.c

sample-$(SERVER_SAMPLE) += server
sample-server-$(SERVER_SAMPLE) := server.c

sample-$(SERVER_SSE_SAMPLE) += server-sse
sample-server-sse-$(SERVER_SSE_SAMPLE) := server-sse.c

sample-$(SERVER_HTTPS_SAMPLE) += server-https
sample-server-https-$(SERVER_HTTPS_SAMPLE) := server-https.c
